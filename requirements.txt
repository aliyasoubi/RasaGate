# Rasa Gate — API service dependencies only.
# Rasa itself runs in its own container (see docker-compose.yml) and is NOT a
# dependency of this service.

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
SQLAlchemy>=2.0,<3.0
httpx>=0.27,<1.0
PyYAML>=6.0,<7.0
structlog>=24.1,<26.0

# Production database driver (SQLite needs nothing extra)
psycopg2-binary>=2.9,<3.0
